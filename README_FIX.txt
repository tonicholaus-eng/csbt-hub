CSBT HUB — useSearchParams Suspense hotfix

Fixes Next.js 16 production prerender failure on /nich and proactively wraps /exchange too.

Copy everything inside this folder into the root of your csbt-price-checker project and replace files.
Then run:
  npm run build
