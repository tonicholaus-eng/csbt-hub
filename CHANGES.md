# Replacement changes

- Added a dedicated X button that hides the entire floating NICH assistant.
- The dismissed state lasts for the current browser session.
- Kept the existing chat open/close behavior separate from the new dismiss button.
- Reorganized all floating-assistant files under `src/components/nich/assistant/`.
- Updated imports in the page, API route, global loader, and NICH test script.
- Cleaned the TypeScript configuration.
- Removed broken package commands that referenced missing scripts.
- Removed generated builds, installed dependencies, local secrets, temporary archives, and empty folders from the replacement package.
