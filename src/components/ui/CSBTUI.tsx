import Link from "next/link";
import type { ReactNode } from "react";
type SurfaceTone="default"|"muted"|"smart"|"nich"|"selected";
export function Surface({children,className="",tone="default",as="section"}:{children:ReactNode;className?:string;tone?:SurfaceTone;as?:"section"|"div"|"article"|"aside"}){const C=as;const t=tone==="smart"?"csbt-surface-smart":tone==="nich"?"csbt-surface-nich":tone==="selected"?"csbt-surface-selected":tone==="muted"?"csbt-surface-3":"csbt-surface-1";return <C className={`${t} ${className}`}>{children}</C>}
export function PageHeader({eyebrow,title,description,actions,className=""}:{eyebrow?:string;title:string;description?:string;actions?:ReactNode;className?:string}){return <header className={`csbt-page-header ${className}`}><div className="min-w-0 flex-1">{eyebrow&&<p className="csbt-eyebrow">{eyebrow}</p>}<h1 className="csbt-page-title">{title}</h1>{description&&<p className="csbt-page-description">{description}</p>}</div>{actions&&<div className="flex shrink-0 flex-wrap items-center gap-2">{actions}</div>}</header>}
export function SectionHeader({eyebrow,title,description,action}:{eyebrow?:string;title:string;description?:string;action?:ReactNode}){return <div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div className="min-w-0">{eyebrow&&<p className="csbt-eyebrow">{eyebrow}</p>}<h2 className="csbt-section-title">{title}</h2>{description&&<p className="mt-1.5 max-w-3xl text-sm font-medium leading-6 text-[var(--foreground-muted)]">{description}</p>}</div>{action&&<div className="shrink-0">{action}</div>}</div>}
export function EmptyState({icon="✦",title,description,href,actionLabel,onAction}:{icon?:string;title:string;description:string;href?:string;actionLabel?:string;onAction?:()=>void}){const action=actionLabel&&(href?<Link href={href} className="csbt-btn-secondary mt-5">{actionLabel}</Link>:onAction?<button type="button" onClick={onAction} className="csbt-btn-secondary mt-5">{actionLabel}</button>:null);return <div className="csbt-empty-state"><span className="csbt-empty-icon" aria-hidden="true">{icon}</span><h3 className="mt-4 text-base font-black text-[var(--foreground)]">{title}</h3><p className="mx-auto mt-1.5 max-w-md text-sm font-medium leading-6 text-[var(--foreground-muted)]">{description}</p>{action}</div>}
export function Badge({children,tone="neutral"}:{children:ReactNode;tone?:"neutral"|"gold"|"smart"|"nich"|"good"|"bad"}){return <span className={`csbt-badge csbt-badge-${tone}`}>{children}</span>}
