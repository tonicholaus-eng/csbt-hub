import { SectionHeader } from "./ui/CSBTUI";type Props={totalItems:number};
export default function Stats({totalItems}:Props){const stats=[{mark:"▦",value:`${totalItems.toLocaleString()}+`,title:"Items in one database",description:"Search pets and supported Adopt Me item categories without jumping between separate tools.",detail:"Shared across CSBT tools",tone:"gold"},{mark:"⇄",value:"2 sources",title:"GCash + Elve values",description:"Switch between value systems while keeping comparisons and inventory totals clear.",detail:"Never mixed in one calculation",tone:"smart"},{mark:"✦",value:"One hub",title:"From checking to trading",description:"Inventory, wishlist, demand, Trade Finder, community opinions, history and Nich share one product shell.",detail:"Built around the trading routine",tone:"nich"}] as const;const tone={gold:"bg-[var(--surface-selected)] text-[var(--gold-dark)]",smart:"bg-[var(--surface-smart)] text-[var(--cyan)]",nich:"bg-[var(--surface-nich)] text-[var(--purple)]"} as const;return <section aria-labelledby="stats-title"><SectionHeader eyebrow="One connected ecosystem" title="Built for the full trading routine" description="Use the same data and identity before, during and after a trade instead of treating every tool like a separate product."/><div className="mt-6 grid gap-3 lg:grid-cols-3">{stats.map(s=><article key={s.title} className="csbt-card-hover rounded-[var(--radius-card)] border border-[var(--border)] bg-[var(--surface-2)] p-5 shadow-[var(--shadow-sm)] sm:p-6"><span className={`flex h-11 w-11 items-center justify-center rounded-[13px] text-lg font-black ${tone[s.tone]}`}>{s.mark}</span><p className="mt-6 text-3xl font-black tracking-[-.04em] text-[var(--foreground)]">{s.value}</p><h3 className="mt-2 text-lg font-black text-[var(--foreground)]">{s.title}</h3><p className="mt-2 text-sm leading-6 text-[var(--foreground-muted)]">{s.description}</p><p className="mt-5 border-t border-[var(--border)] pt-4 text-xs font-bold text-[var(--foreground-muted)]">{s.detail}</p></article>)}</div></section>}
